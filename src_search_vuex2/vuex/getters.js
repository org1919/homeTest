export const getters = {
    isFirt(state) {
        return state.isFirt;
    },
    isLoading(state) {
        return state.isLoading;
    },
    errMsg(state) {
        return state.errMsg;
    },
    users(state) {
        return state.users;
    }
}